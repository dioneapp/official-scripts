{
  "dependencies": {
    "git": {
      "version": "latest"
    },
    "uv": {
      "version": "latest"
    }
  },
  "installation": [
    {
      "name": "Cloning ComfyUI repository",
      "commands": [
        "git clone https://github.com/comfyanonymous/ComfyUI.git ComfyUI"
      ]
    },
    {
      "name": "Installing PyTorch and Python dependencies",
      "env": {
        "name": ".venv",
        "version": "3.10"
      },
      "commands": [
        {
          "platform": "mac",
          "command": "cd ComfyUI && set \"HF_HUB_CACHE=%cd%\\.\\.cache\" && set \"HF_HOME=%cd%\\.\\.cache\" && uv pip install pip && uv pip install torch==2.8.0 torchvision==0.23.0 torchaudio==2.8.0 && uv pip install -r requirements.txt && uv pip install GitPython pyyaml"
        },
        {
          "platform": "windows",
          "command": "cd ComfyUI && set \"HF_HUB_CACHE=%cd%\\.\\.cache\" && set \"HF_HOME=%cd%\\.\\.cache\" && uv pip install pip && uv pip install torch==2.8.0 torchvision==0.23.0 torchaudio==2.8.0 --index-url https://download.pytorch.org/whl/cu129 && uv pip install -r requirements.txt && uv pip install GitPython pyyaml"
        },
        {
          "platform": "linux",
          "command": "cd ComfyUI && set \"HF_HUB_CACHE=%cd%\\.\\.cache\" && set \"HF_HOME=%cd%\\.\\.cache\" && uv pip install pip && uv pip install torch==2.8.0 torchvision==0.23.0 torchaudio==2.8.0 --index-url https://download.pytorch.org/whl/cu129 && uv pip install -r requirements.txt && uv pip install GitPython pyyaml"
        }
      ]
    },
    {
      "name": "Installing ComfyUI Manager",
      "commands": [
        "cd ComfyUI/custom_nodes",
        "git clone https://github.com/ltdrdata/ComfyUI-Manager.git"
      ]
    }
  ],
  "start": [
    {
      "name": "Default Start",
      "catch": "8188",
      "env": ".venv",
      "commands": [
        "cd ComfyUI",
        "set \"HF_HUB_CACHE=%cd%\\.\\.cache\" && set \"HF_HOME=%cd%\\.\\.cache\" && python main.py --listen 127.0.0.1 --port 8188 --disable-auto-launch"
      ]
    },
    {
      "name": "Start with Params",
      "catch": "8288",
      "env": ".venv",
      "steps": [
        {
          "name": "Starting ComfyUI",
          "commands": [
            "cd ComfyUI",
            "set \"HF_HUB_CACHE=%cd%\\.\\.cache\" && set \"HF_HOME=%cd%\\.\\.cache\"",
            { "command": "python main.py --port 8288", "customizable": true }
        ]
        }
    ]
    }
  ]
}
